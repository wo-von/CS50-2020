// To test how an array is initialized in tideman question regaring preferences 2d array

#include <stdio.h>

int main(void)
{
    int sina[10];
    for (int i = 0; i < 10; i++)
    {
        printf("%i\n", sina[i]);
    }
    return 0;
}